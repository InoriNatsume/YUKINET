# YUKINET — 프로젝트 가이드

https://inorinatsume.github.io/YUKINET/

> **AI 딸깍질이 수학과 코드를 만날 때, 이야기는 시작된다.**

---

## 이 프로젝트가 하는 일

AI 이미지·영상 생성 기술(Diffusion 계열)의 **수학**과 **코드**를 분석해서 문서 사이트로 정리하는 프로젝트입니다.

"이 하이퍼파라미터가 무슨 뜻이지?", "이 샘플러가 수학적으로 뭘 하는 거지?", "LoRA lr을 왜 이 값으로 잡지?" 같은 질문에 **수식과 코드 수준**에서 답하는 걸 목표로 합니다.

사이트는 [MkDocs Material](https://squidfunk.github.io/mkdocs-material/) 기반이고, GitHub Pages로 배포합니다.

---

## 4계층 콘텐츠 구조

사이트의 모든 콘텐츠는 아래 4개 계층으로 나뉩니다.  
**같은 내용을 여러 곳에 중복 작성하지 않기 위한** 설계입니다.

```
📐 이론 (Theory)     ─  수학·알고리즘을 여기서 한 번만 정의
      ↓ 참조
🧩 모델 (Models)     ─  각 모델이 이론의 부품을 어떻게 조합하는지
      ↓ 참조
🔧 도구 (Tools)      ─  각 도구가 모델을 코드로 어떻게 구현하는지
      ↓ 실험
🧪 실험 (Lab)        ─  실제로 돌려보고 검증한 결과·레시피
```

| 계층 | 역할 | 예시 |
|---|---|---|
| **📐 이론** | 보편적인 수식·알고리즘 정의 | 확산 수학, 샘플러 카탈로그, 스케줄러 카탈로그, 옵티마이저, LoRA 수학 |
| **🧩 모델** | 모델별 아키텍처·확산 방식 분석 | Flux, SDXL, SD3, Wan 등 |
| **🔧 도구** | 도구별 코드 수준 분석 | ComfyUI, kohya, diffusion-pipe 등 |
| **🧪 실험** | 실전 훈련·추론 결과 기록 | 세팅, 비교, 검증된 레시피 |

**원칙**: 수식을 수정할 일이 생기면 **이론 페이지 한 곳만** 고치면 됩니다. 모델·도구·실험 페이지는 링크로 참조하고 있어서 자동으로 반영됩니다.

> 💡 각 계층에 포함되는 항목은 계속 추가됩니다. 새 모델, 새 샘플러, 새 도구가 나오면 해당 계층에 페이지를 추가하면 됩니다. 추가 방법은 아래 [콘텐츠 추가 가이드](#콘텐츠-추가-가이드)를 참고하세요.

---

## 디렉토리 구조

```
kohya_anima/
├── .github/workflows/deploy-pages.yml    # GitHub Pages 자동 배포
├── homepage/
│   ├── mkdocs.yml                        # ★ 사이트 설정 (네비게이션, 확장 등)
│   ├── config/site_settings.yml          # ★ 버전·변수 단일 소스
│   ├── requirements.txt                  # Python 의존성
│   ├── main.py                           # mkdocs-macros 훅
│   ├── venv/                             # 로컬 가상환경
│   ├── docs/                             # 문서 원본
│   │   ├── index.md                      # 홈 페이지
│   │   ├── stylesheets/extra.css         # 커스텀 디자인 (CSS)
│   │   ├── javascripts/mathjax.js        # 수식(MathJax) 설정
│   │   ├── theory/                       # 📐 이론 계층
│   │   ├── models/                       # 🧩 모델 계층
│   │   ├── tools/                        # 🔧 도구 계층
│   │   └── lab/                          # 🧪 실험 계층
│   └── site/                             # 빌드 결과물 (배포용)
├── Reference/                            # 참조 코드 모음
│   ├── ComfyUI-*/                        # 분석 대상 코드 (버전별)
│   ├── sd-scripts-*/                     # 분석 대상 코드 (버전별)
│   ├── DiffSynth-Studio/                 # 분석 대상 코드
│   └── diffusion-pipe/                   # 분석 대상 코드
└── Exp/                                  # 실험/임시 산출물
```

---

## 버전 관리

모든 분석 대상의 **버전 번호**는 아래 파일 한 곳에서만 관리합니다:

📄 **`homepage/config/site_settings.yml`**

```yaml
repo_versions:
  comfyui: "x.x.x"
  sdscripts: "x.x.x"
  diffsynth: "x.x.x"
  diffusers: "x.x.x"
  transformers: "x.x.x"
  diffusion_pipe: "x.x.x"
```

문서 안에서는 이렇게 참조합니다:

```markdown
ComfyUI v{{ ver.comfyui }}에서 확인된 코드입니다.
```

**버전이 올라가면** → `site_settings.yml` 한 파일만 수정하면 사이트 전체에 자동 반영됩니다.

> ⚠️ README나 문서에 버전 숫자를 직접 적지 마세요. 반드시 `{{ ver.xxx }}` 변수를 사용하세요.  
> (단, README.md 같은 루트 파일은 MkDocs 밖이라 변수 치환이 안 됩니다. 버전을 언급해야 한다면 `site_settings.yml`을 참조하라고 안내하세요.)

---

## 분석 대상 코드베이스

아래 코드베이스를 분석 대상으로 삼고 있으며, 필요에 따라 추가될 수 있습니다.  
각 코드의 **분석 버전**은 `homepage/config/site_settings.yml`에서 확인하세요.

| 코드베이스 | 역할 | GitHub |
|---|---|---|
| ComfyUI | 추론 (노드 기반 워크플로) | [comfyanonymous/ComfyUI](https://github.com/comfyanonymous/ComfyUI) |
| sd-scripts (kohya) | 훈련 (CLI 기반, LoRA/DreamBooth) | [kohya-ss/sd-scripts](https://github.com/kohya-ss/sd-scripts) |
| diffusion-pipe | 훈련 (Pipeline Parallel) | [tdrussell/diffusion-pipe](https://github.com/tdrussell/diffusion-pipe) |
| DiffSynth-Studio | 훈련+추론 (통합) | [modelscope/DiffSynth-Studio](https://github.com/modelscope/DiffSynth-Studio) |
| HuggingFace diffusers | 훈련+추론 (Pipeline) | [huggingface/diffusers](https://github.com/huggingface/diffusers) |
| HuggingFace transformers | 모델 아키텍처 | [huggingface/transformers](https://github.com/huggingface/transformers) |

---

## 로컬 실행

```powershell
cd homepage

# 최초 1회: 가상환경 + 의존성 설치
python -m venv venv
.\venv\Scripts\pip install -r requirements.txt

# 개발 서버 (http://127.0.0.1:8000/)
.\venv\Scripts\mkdocs serve

# 빌드만 (CI와 동일)
.\venv\Scripts\mkdocs build
```

---

## 배포

`main` 브랜치에 push하면 GitHub Actions가 자동으로 빌드 → GitHub Pages에 배포합니다.

- 워크플로 파일: `.github/workflows/deploy-pages.yml`
- 트리거 조건: `homepage/` 폴더 내 파일이 변경될 때

---

## 콘텐츠 추가 가이드

### 새 모델 추가

1. `docs/models/모델명.md` 파일 생성
2. HuggingFace 모델 카드 링크, 아키텍처·확산 방식 등 "부품 조합" 표 작성
3. 도구별 지원 여부 표에 `{{ ver.xxx }}` 변수 사용
4. `mkdocs.yml`의 `nav` → `🧩 모델` 아래에 항목 추가
5. `docs/models/index.md` 목록에 추가

### 새 도구 추가

1. `docs/tools/도구명/index.md` 파일 생성
2. `homepage/config/site_settings.yml` → `repo_versions`에 버전 키 추가
3. `mkdocs.yml`의 `nav` → `🔧 도구` 아래에 항목 추가
4. `docs/tools/index.md`에 섹션 추가

### 새 샘플러·옵티마이저 추가

1. 해당 카탈로그 폴더에 `.md` 파일 생성
   - 샘플러: `docs/theory/samplers/sampler/이름.md`
   - 스케줄러: `docs/theory/samplers/scheduler/이름.md`
   - 옵티마이저: `docs/theory/optimizers/이름.md`
2. `mkdocs.yml`의 `nav` → 해당 카탈로그 아래에 항목 추가

### 기존 콘텐츠 수정

- **수식·알고리즘 변경** → 이론 페이지 한 곳만 수정 (참조 구조라 자동 반영)
- **버전 변경** → `site_settings.yml` 한 곳만 수정

---

## 디자인/설정 가이드

디자인 규칙과 MkDocs 세부 설정은 아래 문서를 기준으로 관리합니다.

- `DESIGN_GUIDE.md`

---

## 알려진 사항

| 항목 | 상태 | 비고 |
|---|---|---|
| MkDocs 2.0 경고 | 무해 | mkdocs-material이 띄우는 광고성 메시지, 무시해도 됨 |
| Anima 모델 | Preview 단계 | LoRA 호환성 미보장, 가중치 변경 예정 |
| 구 HTML 사이트 | 보관 중 | `sampler_site/`, `training_site/` — 마이그레이션 완료, 원본 참조용으로 남겨둠 |
